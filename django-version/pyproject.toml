[project]
name = "django-version"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.1",
    "psycopg[binary]>=3.3.2",
    "python-dotenv>=1.2.1",
]
